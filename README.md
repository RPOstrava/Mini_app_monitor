A simple web application that displays the current time and number of accesses.

The basis is an application (Python-Flask), which we will gradually:

Dockerize - Docker

Run in Kubernetes - AWS EKS

Connect the CI/CD pipeline from GitLab

Finally, we will extend it with Terraform, Prometheus and Grafana.